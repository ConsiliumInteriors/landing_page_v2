# Landing/Contact page for consiliuminteriors.com (2021)
### Build and Deploy (hosted on firebase)
```bash
# Install firebase-tools 
npm install -f firebase-tools

# Build project
npm run build

# Login to firebase
firebase login

# Deploy build
firebase deploy --project consilium-interiors

```
